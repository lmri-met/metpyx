[project]
name = "metpyx"
version = "2.0.0"
description = 'A toolkit for x-ray metrology'
authors = [
  { name = "Xandra Campo", email = "xandra.campo@ciemat.es" }
]
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"